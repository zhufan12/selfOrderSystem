spring:
  cloud:
    loadbalancer:
      ribbon:
        enable: false
      enabled: true
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/selforde?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  jpa:
    show-sql: true
  rabbitmq:
    username: admin
    password: admin123
    host: localhost
    port: 5672
  data:
    redis:
      host: localhost
      port: 6379




eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8082

